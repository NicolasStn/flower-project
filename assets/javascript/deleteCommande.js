let btn = document.getElementById('deleteCommandeBtn');
let deleteCommande = document.getElementById('deleteCommandeForm');

btn.addEventListener('click', function () {
    deleteCommande.style.display = 'flex';
})